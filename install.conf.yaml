- defaults:
    link:
      relink: true
      create: true

- clean: ["~"]

- link:
    ~/.dotfiles: ""
    ~/.vimrc:
    ~/.zshrc:
    ~/.gitconfig:
    ~/.gitignore_global:

- create:
    - ~/.vim/undo-history

- shell:
    - [git submodule update --init --recursive, Installing submodules]
